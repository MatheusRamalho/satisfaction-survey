import styled from "styled-components";

export const HeaderRootWrapper = styled.header`
    width: 100%;
    max-width: 750px;
    height: auto;
    margin-bottom: ${props => props.theme.sizes.s18};
    text-align: center;
`;
